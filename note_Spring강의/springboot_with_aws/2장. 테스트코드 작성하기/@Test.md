### @Test의 종류

- ```
  import org.junit.Test;
  ```

  - 현재 코드 상에서 사용하는 테스트 어노테이션

  

- ```
  import org.junit.jupiter.api.Test
  ```

  - test를 import하려 할 때 맨 위에 뜨는 test. 이걸 하면 오류가 발생하니까,위에서 말한 test 어노테이션 사용하기.